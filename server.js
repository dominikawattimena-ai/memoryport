import http from 'node:http';
const PORT=process.env.PORT||10000;const BASE=process.env.PUBLIC_BASE_URL||'';const j=(res,s,b,h={})=>{res.writeHead(s,{'content-type':'application/json','cache-control':'no-store',...h});res.end(JSON.stringify(b))};const server=http.createServer(async(req,res)=>{const u=new URL(req.url,'http://x');const base=BASE||('https://'+(req.headers.host||''));if(u.pathname==='/'&&req.method==='GET'){res.writeHead(200,{'content-type':'text/html'});return res.end('<!doctype html><meta charset="utf-8"><title>MemoryPort</title><h1>MemoryPort Claude Bridge</h1><p>ONE CARD · ONE MEMORY · ANY AI</p>')}if(u.pathname==='/.well-known/oauth-protected-resource')return j(res,200,{resource:base+'/mcp',authorization_servers:[base],scopes_supported:['memory:read','memory:write'],bearer_methods_supported:['header']});if(u.pathname==='/.well-known/oauth-authorization-server')return j(res,200,{issuer:base,authorization_endpoint:base+'/authorize',token_endpoint:base+'/token',response_types_supported:['code'],grant_types_supported:['authorization_code'],code_challenge_methods_supported:['S256'],scopes_supported:['memory:read','memory:write']});if(u.pathname==='/authorize') {res.writeHead(503,{'content-type':'text/html','cache-control':'no-store'});return res.end('<h1>MemoryPort</h1><p>Owner authorization is fail-closed until canonical candidate/approval pairing is deployed.</p>')}if(u.pathname==='/token')return j(res,503,{error:'temporarily_unavailable'});if(u.pathname==='/mcp'&&req.method==='POST'){let raw='';for await(const c of req)raw+=c;let b;try{b=JSON.parse(raw||'{}')}catch{return j(res,400,{jsonrpc:'2.0',id:null,error:{code:-32700,message:'Parse error'}})}if(b.method==='initialize')return j(res,200,{jsonrpc:'2.0',id:b.id??null,result:{protocolVersion:b.params?.protocolVersion||'2025-06-18',capabilities:{tools:{}},serverInfo:{name:'MemoryPort',version:'1.0.0'}}});if(b.method==='notifications/initialized'){res.writeHead(204);return res.end()}if(b.method==='ping')return j(res,200,{jsonrpc:'2.0',id:b.id??null,result:{}});const auth=req.headers.authorization||'';if(!/^Bearer /i.test(auth))return j(res,401,{error:'unauthorized'},{'www-authenticate':'Bearer resource_metadata="'+base+'/.well-known/oauth-protected-resource"'});if(b.method==='tools/list')return j(res,200,{jsonrpc:'2.0',id:b.id??null,result:{tools:[{name:'memoryport_get_context',description:'Read owner-approved MemoryPort context.',inputSchema:{type:'object',properties:{}}},{name:'memoryport_submit_candidate',description:'Submit candidate memory for owner approval; never direct-write canonical memory.',inputSchema:{type:'object',properties:{content:{type:'string'}},required:['content']}}]}});return j(res,501,{jsonrpc:'2.0',id:b.id??null,error:{code:-32001,message:'Canonical authenticated bridge not active'}})}return j(res,404,{error:'not_found'})});server.listen(PORT,()=>console.log('MemoryPort bridge listening',PORT));
